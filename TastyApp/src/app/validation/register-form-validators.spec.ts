import { RegisterFormValidators } from './register-form-validators';

describe('RegisterFormValidators', () => {
  it('should create an instance', () => {
    expect(new RegisterFormValidators()).toBeTruthy();
  });
});
