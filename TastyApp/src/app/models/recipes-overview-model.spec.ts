import { RecipesOverviewModel } from './recipes-overview-model';

describe('RecipesOverviewModel', () => {
  it('should create an instance', () => {
    expect(new RecipesOverviewModel()).toBeTruthy();
  });
});
