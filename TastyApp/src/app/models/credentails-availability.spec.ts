import { CredentailsAvailability } from './credentails-availability';

describe('CredentailsAvailability', () => {
  it('should create an instance', () => {
    expect(new CredentailsAvailability()).toBeTruthy();
  });
});
