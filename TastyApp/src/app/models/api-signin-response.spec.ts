import { ApiSigninResponse } from './api-signin-response';

describe('ApiSigninResponse', () => {
  it('should create an instance', () => {
    expect(new ApiSigninResponse()).toBeTruthy();
  });
});
